// Production API Configuration
// Uncomment and set your production API URL here
// This will be loaded before app.js

// Option 1: Set directly
// window.PRODUCTION_API_URL = 'https://your-api.railway.app/api/v1';

// Option 2: Use environment-specific URLs
// if (window.location.hostname.includes('vercel.app')) {
//     window.PRODUCTION_API_URL = 'https://your-production-api.com/api/v1';
// }

// Option 3: Read from meta tag (set in index.html)
// const apiUrlMeta = document.querySelector('meta[name="api-url"]');
// if (apiUrlMeta) {
//     window.PRODUCTION_API_URL = apiUrlMeta.content;
// }
